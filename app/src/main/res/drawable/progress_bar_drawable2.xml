<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="0dp" />

            <gradient
                android:angle="270"
                android:centerColor="@drawable/progress_back"
                android:centerY="0.75"
                android:endColor="@drawable/progress_back"
                android:startColor="@drawable/progress_back" />
        </shape>
    </item>


    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="0dip" />

                <gradient
                    android:angle="270"
                    android:centerColor="@color/sz_yellow"
                    android:centerY="0.75"
                    android:endColor="@color/sz_yellow"
                    android:startColor="@color/sz_yellow" />
            </shape>
        </clip>
    </item>

</layer-list>
